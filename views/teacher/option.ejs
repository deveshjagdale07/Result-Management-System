<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Options</title>
    <style>
        /* General Styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f7fafc;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            text-align: center;
            color: #333;
        }

        /* Header Styling */
        h1 {
            font-size: 2.5em;
            color: #34495e;
            margin: 0;
        }
        
        p {
            font-size: 1.1em;
            color: #777;
            margin-top: 10px;
            margin-bottom: 30px;
        }

        /* Logout Button Styling */
        .btn-logout {
            background-color: #e74c3c;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 16px;
            position: absolute;
            top: 20px;
            right: 20px;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .btn-logout:hover {
            background-color: #c0392b;
        }

        /* Option Buttons Styling */
        .option-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
            flex-wrap: wrap;
        }

        .option-button {
            background-color: #3498db;
            border: none;
            padding: 20px;
            border-radius: 8px;
            font-size: 18px;
            text-transform: uppercase;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 180px;
            height: 150px;
            text-align: center;
        }

        .option-button:hover {
            background-color: #2980b9;
            transform: translateY(-5px);
        }

        .option-button i {
            font-size: 40px;
            margin-bottom: 10px;
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .option-container {
                flex-direction: column;
                gap: 15px;
            }

            .option-button {
                width: 100%;
            }
        }
    </style>

    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>

    <!-- Logout Button -->
    <a href="/" class="btn-logout">Logout</a>

    <!-- Header Section -->
    <header>
        <h1>Welcome, Teacher</h1>
        <p>Manage your students' information with ease. Select an option below:</p>
    </header>

    <!-- Option Buttons -->
    <div class="option-container">
        <a href="/teacher/viewall" class="option-button">
            <i class="fas fa-users"></i>
            View All Students
        </a>
        <a href="/teacher/add" class="option-button">
            <i class="fas fa-user-plus"></i>
            Add New Student
        </a>
    </div>

</body>
</html>
